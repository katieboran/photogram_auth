<div class="page-header">
  <h3> <%= @user.username %></h3>
</div>

<table class="table table-striped table-hover">
  <% Photo.where(:user_id=>@user.id).each do |photo| %>
  <tr>
    <td align = "center"><img src="<%= photo.image %>" width = "400"></td>
  </tr>
  <% end %>
</table>
